name: release-plz

on:
  push:
    branches:
      - master

permissions:
  contents: read

concurrency:
  group: release-plz-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # Publish crates and create tags and GitHub releases when a release PR merges.
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write # push tags, create releases
      id-token: write # crates.io trusted publishing via OIDC
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: DeterminateSystems/nix-installer-action@main
      - uses: DeterminateSystems/magic-nix-cache-action@v13
      - uses: Swatinem/rust-cache@v2
      - id: auth
        uses: rust-lang/crates-io-auth-action@v1
      - run: nix develop -c release-plz release
        env:
          # release-plz reads the git-forge token from GIT_TOKEN.
          GIT_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}

  # Keep the version-bump and changelog PR up to date on every push to master.
  release-pr:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: DeterminateSystems/nix-installer-action@main
      - uses: DeterminateSystems/magic-nix-cache-action@v13
      - uses: Swatinem/rust-cache@v2
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      - run: nix develop -c release-plz release-pr
        env:
          GIT_TOKEN: ${{ secrets.GITHUB_TOKEN }}
