(graph demo-pattern
  (tick-bang0 (tick-bang #:rate 60))
  (number1 (number #:min 0.5))
  (pmini2 (pmini "220 [330 440] <550 660>"))
  (ref3 (ref pwindow #:sync))
  (ref4 (ref pquery #:sync))
  (ref5 (ref psched #:sync))
  (~sinosc6 ~sinosc)
  (~out7 ~out)
  (bang8 bang)
  (pplot9 (pplot #:end 2 #:width 240))
  (-> tick-bang0 bang8)
  (-> bang8 number1)
  (-> tick-bang0 pmini2)
  (-> tick-bang0 ref3)
  (-> number1 (ref3 1))
  (-> number1 (ref5 2))
  (-> pmini2 ref4)
  (-> pmini2 pplot9)
  (-> ref3 (ref4 1))
  (-> ref4 ref5)
  (-> ref3 (ref5 1))
  (-> ref5 ~sinosc6)
  (-> ~sinosc6 ~out7))

(graph demo-pplot
  (tick-bang0 (tick-bang #:rate 30))
  (pmini1 (pmini "0 [1 2] <3 5>"))
  (pplot2 (pplot #:end 2 #:width 240))
  (pmini3 (pmini "bd [sn hh] <cp ~>"))
  (pplot4 (pplot #:end 2 #:width 240))
  (pmini5 (pmini "[bd*2, hh*4]"))
  (pplot6 (pplot #:width 240))
  (expr7 (expr (begin $b (pat/fastcat (list (pat/pure #t) (pat/pure #f) (pat/pure #t) (pat/pure #t)))) #:require "gantz/pattern"))
  (pplot8 (pplot #:width 240))
  (ref9 (ref psaw #:sync))
  (pplot10 (pplot #:fit 2 #:width 240))
  (expr11 (expr (pat/map (lambda (n) (hash 's (if (> n 2) 'sn 'bd) 'n n)) $p) #:require "gantz/pattern"))
  (pplot12 (pplot #:end 2 #:width 240))
  (pplot13 (pplot #:end 2 #:expand #:width 240 #:height 120))
  (expr14 (expr (pat/map (lambda (n) (list n (* n 2))) $p) #:require "gantz/pattern"))
  (pplot15 (pplot #:end 2 #:expand #:width 240 #:height 120))
  (expr16 (expr (pat/map (lambda (n) (lambda () n)) $p) #:require "gantz/pattern"))
  (pplot17 (pplot #:width 240))
  (-> tick-bang0 pmini1)
  (-> tick-bang0 pmini3)
  (-> tick-bang0 pmini5)
  (-> tick-bang0 expr7)
  (-> tick-bang0 ref9)
  (-> pmini1 pplot2)
  (-> pmini3 pplot4)
  (-> pmini5 pplot6)
  (-> expr7 pplot8)
  (-> ref9 pplot10)
  (-> pmini1 expr11)
  (-> expr11 pplot12)
  (-> expr11 pplot13)
  (-> pmini1 expr14)
  (-> expr14 pplot15)
  (-> pmini1 expr16)
  (-> expr16 pplot17))

(graph papp
  (expr0 (expr (pat/app $pv $pf) #:require "gantz/pattern"))
  (inlet1 (inlet "pattern" "pattern of values"))
  (inlet2 (inlet "pattern" "pattern of functions"))
  (outlet3 (outlet "pattern" "functions applied at intersections"))
  (-> inlet1 expr0)
  (-> inlet2 (expr0 1))
  (-> expr0 outlet3))

(graph peuclid
  (expr0 (expr (pat/euclid (exact (round $k)) (exact (round $n))) #:require "gantz/pattern"))
  (inlet1 (inlet "number" "onset count"))
  (inlet2 (inlet "number" "slot count"))
  (outlet3 (outlet "pattern" "one event per onset slot"))
  (-> inlet1 expr0)
  (-> inlet2 (expr0 1))
  (-> expr0 outlet3))

(graph peuclidfull
  (expr0 (expr (pat/euclid-full (exact (round $k)) (exact (round $n))) #:require "gantz/pattern"))
  (inlet1 (inlet "number" "onset count"))
  (inlet2 (inlet "number" "slot count"))
  (outlet3 (outlet "pattern" "onsets elongated to the next onset"))
  (-> inlet1 expr0)
  (-> inlet2 (expr0 1))
  (-> expr0 outlet3))

(graph peuclidoff
  (expr0 (expr (pat/euclid-off (exact (round $k)) (exact (round $n)) (exact (round $off))) #:require "gantz/pattern"))
  (inlet1 (inlet "number" "onset count"))
  (inlet2 (inlet "number" "slot count"))
  (inlet3 (inlet "number" "rotation in slots"))
  (outlet4 (outlet "pattern" "one event per rotated onset slot"))
  (-> inlet1 expr0)
  (-> inlet2 (expr0 1))
  (-> inlet3 (expr0 2))
  (-> expr0 outlet4))

(graph pfast
  (expr0 (expr (pat/fast (pat/rationalize $rate) $p) #:require "gantz/pattern"))
  (inlet1 (inlet "number" "speed factor"))
  (inlet2 (inlet "pattern" "input pattern"))
  (outlet3 (outlet "pattern" "sped-up pattern"))
  (-> inlet1 expr0)
  (-> inlet2 (expr0 1))
  (-> expr0 outlet3))

(graph pfastcat
  (expr0 (expr (pat/fastcat (if (list? $ps) $ps (list $ps))) #:require "gantz/pattern"))
  (inlet1 (inlet "pattern(s)" "patterns in connection order"))
  (outlet2 (outlet "pattern" "all patterns within one cycle"))
  (-> inlet1 expr0)
  (-> expr0 outlet2))

(graph pfilter
  (expr0 (expr (pat/filter $f $p) #:require "gantz/pattern"))
  (inlet1 (inlet "function" "value predicate"))
  (inlet2 (inlet "pattern" "input pattern"))
  (outlet3 (outlet "pattern" "events whose values satisfy the predicate"))
  (-> inlet1 expr0)
  (-> inlet2 (expr0 1))
  (-> expr0 outlet3))

(graph pmap
  (expr0 (expr (pat/map $f $p) #:require "gantz/pattern"))
  (inlet1 (inlet "function" "value mapping"))
  (inlet2 (inlet "pattern" "input pattern"))
  (outlet3 (outlet "pattern" "pattern of mapped values"))
  (-> inlet1 expr0)
  (-> inlet2 (expr0 1))
  (-> expr0 outlet3))

(graph ppure
  (expr0 (expr (pat/pure $v) #:require "gantz/pattern"))
  (inlet1 (inlet "value" "event value"))
  (outlet2 (outlet "pattern" "one event per cycle"))
  (-> inlet1 expr0)
  (-> expr0 outlet2))

(graph pquery
  (expr0 (expr (pat/query $p $span) #:require "gantz/pattern"))
  (inlet1 (inlet "pattern" "pattern to query"))
  (inlet2 (inlet "span" "span to query over"))
  (outlet3 (outlet "events" "events sorted by active start"))
  (-> inlet1 expr0)
  (-> inlet2 (expr0 1))
  (-> expr0 outlet3))

(graph psaw
  (expr0 (expr (begin $b pat/saw) #:require "gantz/pattern"))
  (inlet1 (inlet "bang" "trigger"))
  (outlet2 (outlet "pattern" "0 to 1 ramp each cycle"))
  (-> inlet1 expr0)
  (-> expr0 outlet2))

(graph psched
  (expr0 (expr (pat/events->secs $events $span (hash-ref %args 'time) $cps) #:require "gantz/pattern"))
  (inlet1 (inlet "events" "queried window events"))
  (inlet2 (inlet "span" "the queried window"))
  (inlet3 (inlet "number" "tempo in cycles per second"))
  (outlet4 (outlet "list" "(seconds value) onset pairs"))
  (-> inlet1 expr0)
  (-> inlet2 (expr0 1))
  (-> inlet3 (expr0 2))
  (-> expr0 outlet4))

(graph pshift
  (expr0 (expr (pat/shift (pat/rationalize $amount) $p) #:require "gantz/pattern"))
  (inlet1 (inlet "number" "cycles to shift later"))
  (inlet2 (inlet "pattern" "input pattern"))
  (outlet3 (outlet "pattern" "shifted pattern"))
  (-> inlet1 expr0)
  (-> inlet2 (expr0 1))
  (-> expr0 outlet3))

(graph psilence
  (expr0 (expr (begin $b pat/silence) #:require "gantz/pattern"))
  (inlet1 (inlet "bang" "trigger"))
  (outlet2 (outlet "pattern" "no events"))
  (-> inlet1 expr0)
  (-> expr0 outlet2))

(graph pslow
  (expr0 (expr (pat/slow (pat/rationalize $rate) $p) #:require "gantz/pattern"))
  (inlet1 (inlet "number" "slow-down factor"))
  (inlet2 (inlet "pattern" "input pattern"))
  (outlet3 (outlet "pattern" "slowed pattern"))
  (-> inlet1 expr0)
  (-> inlet2 (expr0 1))
  (-> expr0 outlet3))

(graph pslowcat
  (expr0 (expr (pat/slowcat (if (list? $ps) $ps (list $ps))) #:require "gantz/pattern"))
  (inlet1 (inlet "pattern(s)" "patterns in connection order"))
  (outlet2 (outlet "pattern" "one pattern per cycle"))
  (-> inlet1 expr0)
  (-> expr0 outlet2))

(graph pstack
  (expr0 (expr (pat/stack (if (list? $ps) $ps (list $ps))) #:require "gantz/pattern"))
  (inlet1 (inlet "pattern(s)" "patterns in connection order"))
  (outlet2 (outlet "pattern" "layered patterns"))
  (-> inlet1 expr0)
  (-> expr0 outlet2))

(graph psteady
  (expr0 (expr (pat/steady $v) #:require "gantz/pattern"))
  (inlet1 (inlet "value" "constant value"))
  (outlet2 (outlet "pattern" "continuous constant signal"))
  (-> inlet1 expr0)
  (-> expr0 outlet2))

(graph pwindow
  (expr0 (expr (begin $tick (let ((r (pat/window state (hash-ref %args 'time) $cps))) (begin (set! state (list-ref r 1)) (list-ref r 0)))) #:require "gantz/pattern"))
  (inlet1 (inlet "bang" "tick trigger"))
  (inlet2 (inlet "number" "tempo in cycles per second"))
  (outlet3 (outlet "span" "the window since the previous tick"))
  (-> inlet1 expr0)
  (-> inlet2 (expr0 1))
  (-> expr0 outlet3))

(descriptions
  (demo-pattern "A mini-notation frequency pattern queried per tick plays a sine and plots two cycles. Edit the string and dial the cps.")
  (demo-pplot "Every kind of event value in pplots. Numbers, symbols, overlapping symbols, bools, a signal, maps stacked and expanded, lists and opaque values.")
  (papp "Apply a pattern of functions to a pattern of values.")
  (peuclid "K onsets distributed over N slots per cycle.")
  (peuclidfull "Euclidean onsets elongated to fill the gaps.")
  (peuclidoff "Euclidean onsets rotated left by an offset.")
  (pfast "Speed a pattern up by a factor.")
  (pfastcat "Concatenate patterns within a single cycle.")
  (pfilter "Keep events whose values satisfy a predicate.")
  (pmap "Map a function over a pattern's values.")
  (ppure "Repeat a value once per cycle.")
  (pquery "Query a pattern over a span for its sorted events.")
  (psaw "A 0 to 1 ramp signal each cycle.")
  (psched "Window events as (seconds value) onset pairs for delivery.")
  (pshift "Shift a pattern later in time.")
  (psilence "The empty pattern.")
  (pslow "Slow a pattern down by a factor.")
  (pslowcat "Concatenate patterns, one per cycle.")
  (pstack "Layer patterns on top of each other.")
  (psteady "A continuous constant signal.")
  (pwindow "Advance a tick-driven query window in cycles."))

(layout demo-pplot
  (expr11 -40 650)
  (expr14 -40 950)
  (expr16 -40 1120)
  (expr7 -220 390)
  (pmini1 -220 0)
  (pmini3 -220 130)
  (pmini5 -220 260)
  (pplot10 160 520)
  (pplot12 160 650)
  (pplot13 160 780)
  (pplot15 160 950)
  (pplot17 160 1120)
  (pplot2 160 0)
  (pplot4 160 130)
  (pplot6 160 260)
  (pplot8 160 390)
  (ref9 -220 520)
  (tick-bang0 -420 0)
  (camera 0 560 1))
